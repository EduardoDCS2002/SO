struct info{
    int id;
    char* commando;
    char* coisas;
}*info;

// client execute status
void penis(){
    struct info array[20];
    for(int i = 0; i < 20;i++){
        int x = array[i].id;
        char* y = array[i].commando;
    }
}